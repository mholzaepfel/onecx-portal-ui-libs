name: CI Push

on:
  push:
    branches:
      - '*'
      - '*/*'
      - '**'

jobs:
  ci-common:
    uses: ./.github/workflows/ci-common.yml
    secrets: inherit
  ci-sonar:
    uses: ./github/workflows/ci-sonar.yml
    secrets: inherit
